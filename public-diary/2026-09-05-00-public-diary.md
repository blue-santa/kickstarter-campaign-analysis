I had an issue where I couldn't get the confusion matrix to export. I used Google Search with enhanced AI. The tool informed me that I needed to switch `_` to `fig` and then use the `fig` object with `.savefig()` and some parameters. This solved the issue.

---

The entire markdowntopdf script that converts my markdown report to a pdf is ai-generated. I had almost nothing to do with that script, other than a few tweaks. I used Claude, primarily.
